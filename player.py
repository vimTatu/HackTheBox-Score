class Player(object):
    def __init__(self, name, score, users, roots, rank, last, prev):
        self.name = name
        self.score = score
        self.users = users
        self.roots = roots
        self.rank = rank
        self.last = last
        self.prev = prev
